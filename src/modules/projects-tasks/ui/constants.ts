// src/modules/projects-tasks/ui/constants.ts
import { ProjectStatus, TaskStatus, TaskPriority } from '../types';

export const ProjectStatusARR = Object.values(ProjectStatus);
export const TaskStatusARR = Object.values(TaskStatus);
export const TaskPriorityARR = Object.values(TaskPriority);
