// src/modules/knowledge-management/ui/constants.ts
import { ContentType, ContentStatus } from '../types';

export const ContentTypeARR = Object.values(ContentType);
export const ContentStatusARR = Object.values(ContentStatus);

// Add other enum arrays here if needed for future forms/filters
