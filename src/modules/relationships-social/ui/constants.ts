// src/modules/relationships-social/ui/constants.ts
import { InteractionType } from '../types';

export const InteractionTypeARR = Object.values(InteractionType);

// You can add other enum arrays here if needed for other forms/filters
